.DS_Store
__pycache__/
*.pyc

# axe-core (accessibility test tool, scripts/check-accessibility.py) —
# installed via `npm install`, not part of the shipped app.
/node_modules/

# Disposable root-level GitHub Pages build output. The leading slashes are
# intentional: an unanchored `sentences/` also hides Resources/Sentences on a
# case-insensitive filesystem, preventing new source audio from being committed.
# Actions restores these generated pages from its validated render cache.
/word/
/story/
/stories/
/sentences/
/word-game/
/pronunciation/
/updates/
/sitemap.xml
/page-manifest.json
/build/
/.pages-cache/

# Local Firestore emulator / CLI runtime output (npm run emulators, deploy).
firebase-debug.log
firestore-debug.log
ui-debug.log
/.firebase/

# Real personal data pulled from production for local testing — see
# scripts/import-production-data.sh. Must never be committed.
/my-words-debug-export.json
